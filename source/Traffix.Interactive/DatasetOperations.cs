using System.Collections.Generic;
using Traffix.Processors;

namespace Traffix.Interactive
{
    public sealed class DatasetOperations
    {
        private Interactive _interactive;

        internal DatasetOperations(Interactive interactive)
        {
            this._interactive = interactive;
        }

        // Dataset PrepareFromFile(string pcapPath, )
        // IEnumerable<ConversationRecord<TData>> Apply<TData>


        
    }
}